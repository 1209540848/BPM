/**
 * Demo-only notes for the theme system.
 *
 * This file intentionally exports nothing runtime-critical.
 * It exists so the repo keeps a placeholder for theme-system
 * documentation without breaking TypeScript builds.
 */

export const THEME_SYSTEM_DEMO_NOTE = `
Theme system demo placeholder.

Use the real theme implementation from:
- src/theme/index.ts
- src/theme/designTokens.ts
- src/stores/theme.ts
`;

export {};
